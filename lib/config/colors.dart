import 'package:flutter/material.dart';

class AppColors {
  // 🔹 Backgrounds
  static const Color background = Color(0xFF0F172A); // main background
  static const Color surface = Color(0xFF020617); // cards, navbar
  static const Color section = Color(0xFF111827); // sections

  // 🔹 Brand Colors
  static const Color primary = Color(0xFF38BDF8); // sky blue
  static const Color accent = Color(0xFF22C55E); // green (CTA)

  // 🔹 Text Colors
  static const Color textPrimary = Color(0xFFE5E7EB); // main text
  static const Color textSecondary = Color(0xFF9CA3AF); // subtitles
  static const Color textMuted = Color(0xFF6B7280); // hints

  // 🔹 Borders & Dividers
  static const Color border = Color(0xFF1F2933);

  // 🔹 Status Colors
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFDC2626);

  // 🔹 Hover / Effects
  static const Color hover = Color(0xFF1E293B);
}
